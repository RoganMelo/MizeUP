@model MizeUP.Models.InstitutionModel

@{
    ViewBag.Title = Languages.EducationalInstitution;
}

<div class="container">
    <div class="page-header">
        <h2>
            <strong>@Languages.EducationalInstitution&nbsp;-&nbsp;@Languages.Registration</strong>
        </h2>
    </div>
    <div class="form-horizontal">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <div class="col-md-offset-2 col-md-4">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-4">
                    @Html.TextBoxFor(model => model.Name, htmlAttributes: new { autocomplete = "off", @class = "form-control" })
                </div>
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger col-md-6" })
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-4">
                    <button type="submit" class="btn btn-success">@Languages.Save</button>
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {

}